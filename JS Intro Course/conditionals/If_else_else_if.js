let hasAccount = 'No';
let hasWallet = 'Yes';

if (hasAccount === 'Yes') {
    console.log('Citizen is qualified');
} else if (hasWallet === 'Yes') {
    console.log('Citizen is qualified');
} else {
    console.log('Citizen is not qualified');
}
